apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "pinpoint-zookeeper.labels" . | indent 4 }}
  name: {{ include "pinpoint-zookeeper.fullname" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
{{ include "pinpoint-zookeeper.selectorLabels" . | indent 6 }}
  template:
    metadata:
      labels:
{{ include "pinpoint-zookeeper.labels" . | indent 8 }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      hostname: {{ include "pinpoint-zookeeper.fullname" . }}
      dnsPolicy: "Default"
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          env:
            - name: ZOO_SERVERS
              value: server.1={{ include "pinpoint-zookeeper.fullname" . }}:2888:3888;2181
          ports:
          - containerPort: 2181
            protocol: TCP
          livenessProbe:
            failureThreshold: 3
            tcpSocket:
              port: 2181
            initialDelaySeconds: 30
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 2
          readinessProbe:
            failureThreshold: 3
            tcpSocket:
              port: 2181
            initialDelaySeconds: 30
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 2
          resources:
{{- toYaml .Values.resources | nindent 12 }}
{{- with .Values.affinity }}
      affinity:
{{- toYaml . | nindent 8 }}
{{- end }}
{{- with .Values.tolerations }}
      tolerations:
{{- toYaml . | nindent 8 }}
{{- end }}
